/** @type {import('next').NextConfig} */
const nextConfig = {
          output: 'standalone',
          assetPrefix: '/',
          basePath: '',
          trailingSlash: false,
          reactStrictMode: true,
};

export default nextConfig;
